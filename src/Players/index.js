import * as actions from './actions';
import * as constants from './constants';
import * as selectors from './selectors';
import reducer from './reducer';
import PlayersTable from './containers/PlayersTableContainer';

export default {
  actions,
  constants,
  reducer,
  selectors,
  PlayersTable,
};
